type User {
  id: ID,
  name: String,
  username: String,
  email: String!
}

type Auth {
  user: User!,
  token: String!
}

input AuthInput {
  email: String!,
  password: String!
}

type Mutation {
  authUser(data: AuthInput!): Auth!
}
